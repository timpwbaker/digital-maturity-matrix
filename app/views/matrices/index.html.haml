%p#notice= notice
%h1 Listing Matrices
%table
  %thead
    %tr
      %th Name
      %th{:colspan => "3"}
  %tbody
    - @matrices.each do |matrix|
      %tr
        %td= matrix.name
        %td= link_to 'Show', matrix
        %td= link_to 'Edit', edit_matrix_path(matrix)
        %td= link_to 'Destroy', matrix, method: :delete, data: { confirm: 'Are you sure?' }
%br/
= link_to 'New Matrix', new_matrix_path
