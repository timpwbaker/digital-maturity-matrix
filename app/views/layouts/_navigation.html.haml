%nav.navbar.navbar-inverse.navbar-fixed-top
  .container
    .navbar-header
      %button.navbar-toggle{"data-target" => ".navbar-collapse", "data-toggle" => "collapse", :type => "button"}
        %span.sr-only Toggle navigation
        %span.icon-bar
        %span.icon-bar
        %span.icon-bar
    .collapse.navbar-collapse
      %ul.nav.navbar-nav
        %li= link_to 'Home', root_path
        -if !Matrix.exists?(id: 1)
        -elsif !current_user
          %li= link_to 'Create My Maturity Matrix', new_matrix_submission_path(Matrix.find(1))
        -elsif current_user && Submission.exists?(user_id: current_user.id)
          %li= link_to 'Edit My Matrix', edit_matrix_submission_path(Matrix.find(1),Submission.find_by_user_id(current_user.id), :area => "Technology")
          %li= link_to 'View My Matrix', matrix_submission_path(Matrix.find(1),Submission.find_by_user_id(current_user.id))
        -elsif current_user && !Submission.exists?(user_id: current_user.id)
          %li= link_to 'Create Digital Maturity Matrix', new_matrix_submission_path(Matrix.find(1))
        -if user_signed_in?
          %li.dropdown
            %a.dropdown-toggle{:href => "#", "role" => "button", "data-toggle" => "dropdown", "data-target" => "#"} 
              My Account
              %b.caret
            %ul.dropdown-menu{"role" => "menu"}
              %li= link_to 'Edit account', edit_user_registration_path
              %li= link_to 'Sign out', destroy_user_session_path, :method=>'delete'
        - else
          %li= link_to 'Sign in', new_user_session_path