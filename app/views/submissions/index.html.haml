%p#notice= notice
%h1 Listing Submissions
%table
  %thead
    %tr
      %th Matrix
      %th Created by
      %th{:colspan => "3"}
  %tbody
    - submissions.each do |submission|
      -if user_signed_in? && (submission.user == current_user || current_user.admin?)
        %tr
          %td= submission.matrix.name
          %td= submission.user.name
          %td= link_to 'Show', matrix_submission_path(matrix, submission)
          %td= link_to 'Destroy', matrix_submission_path(matrix, submission), method: :delete, data: { confirm: 'Are you sure?' }
%br/
= button_to 'Download export', matrix_submissions_csv_path(matrix)
